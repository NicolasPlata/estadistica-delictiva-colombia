DB_USER=postgres
DB_PASS=changeme
DB_HOST=localhost
DB_PORT=5432
DB_NAME=estadistica_delictiva

# Opcional, default 3000 si no se define (usado por backend/)
SERVER_PORT=3000

# Opcional, default http://localhost:5173 (puerto estándar de `vite dev`)
CORS_ALLOWED_ORIGIN=http://localhost:5173
